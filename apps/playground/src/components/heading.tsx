import {Heading} from "@simplix/ui";
import {JSX} from "react";

export default function HeadingDemo(): JSX.Element {
    return (
        <Heading variant={`h1`}>
            Heading Extra-Large
        </Heading>
    )
}