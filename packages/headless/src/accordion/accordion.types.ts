export type AccordionContextValue = {
    open: boolean;
    setOpen: (open: boolean) => void;
}

export type AccordionState = {
    open?: boolean;
}

export type AccordionStateResult = {
    open: boolean;
    setOpen: (open: boolean) => void;
}